# 全部原子技能：TCP 学生的五技能训练与组合开发实验

> 历史方案：后续已采用 [三粗／七细技能定义](../skill_hierarchy/README.md)。旧五技能训练完成，但原定 evaluation-v6 在策略动作开始前取消；下文的“等待执行”是当时状态，不能当作当前运行安排或已执行结果。七技能训练结果单独记录，不重新解释旧五技能标签。

当前状态：教师采集32/32成功；25mm尺度下两条完整教师TCP接触重放均完成五技能；数据审计通过，训练/开发分别每技能256/32窗口。针对性测试17项通过。训练v4已在云端启动，评估v6等待训练成功退出及checkpoint验证后执行80个单元。该状态不代表学生训练已完成或任务成功。

采集产物35文件、438,055,754字节已回本机逐文件校验，`LOCAL_VERIFIED`；Drive未验证。训练中权重和后续评估尚未作为完成产物归档。评估v5在尚未执行policy动作时取消，v6在准备结束立即复制观察、物理状态及稳定放置计数，避免reset复用观察缓冲区；每对只做一次教师准备，两个条件恢复同一真实起点，交接仍不reset。

2026-09-08 用户要求开始原子技能，并明确“就是全部”。本轮覆盖 reach、grasp、lift、transport、place、四个相邻组合与完整 pick-and-place。保留一个共享学生，使用原任务语言与真实观察，不新增 GT 技能标签输入或五套网络。

已有 Reach 最佳 checkpoint 作为共同起点。先在真实接触桌面环境采集 24 train + 8 新 development 完整教师轨迹，逐步记录 actual/commanded target、真实夹爪目标和技能结果；教师拒绝保留，不替换 seed。历史 D0 缺 commanded target，不能猜测 TCP 标签，因此不直接转换旧 D0。新 val 存储字段明确只代表本轮 tuning development，不读取历史 val/final test。

动作仍为固定 chunk anchor 轴向的 TCP 位姿差分，第一步 actual→command、之后 command→command，16×7、20 Hz、执行前4步；真实夹爪开闭标签不截断。保留0.1 rad计划/修正限制和0.05 rad跟踪检查。接触环境复用项目五技能谓词：Reach为TCP到物体4cm，而旧无碰撞Reach是到上方8cm目标点2cm，结果禁止混合比较。

训练前验证命令来源、时间、掩码、五技能曝光、TCP标签范围与FK往返，并做教师TCP接触重放。无效标签不静默裁剪。新五技能轨迹没有合格Memory记录，显式mask；不在接触后使用抓前静态物体位置。保留旧Reach窗口作为1/6回放，其原Memory语义不变；五技能在线先使用与新训练相符的masked Memory。

冻结Qwen与Adapter，从Reach最佳Expert与Memory encoder起步，AdamW新优化状态、lr=1e-5，先完成4096更新。每次更新五技能各1窗口加旧Reach1窗口，训练每技能256窗口、development每技能32窗口，保留跨技能窗口与合法尾部mask。采用最后一个预定checkpoint，离线误差不称作收敛。保存优化器与随机状态，训练或保存失败不启动依赖评估。

4个新development场景 ×（5原子+4相邻组合+1完整任务）×（续训前/后），计划80个单元。原子只由教师准备前置状态；组合只在起点准备，交接不reset、不补教师。分别报告教师准备失败、实现错误、模型停止、技能成功及组合到达/条件分母。原子160步、相邻240步、完整400步，10步Euler，固定采样seed规则；不是最终独立测试或物理操作。

云端单卡顺序计算；采集、训练和评估设置故障超时并保存已完成证据。本轮是首次五技能训练与验收，不承诺一次达到收敛，不自动无限追加训练。源码隔离在本目录，复用稳定组件，历史权重和证据只读；国内输出回本机校验归档。先验证共同数据与执行链，再启动训练。

前置结果：32/32教师完整任务成功。v2教师TCP重放分别因1.30e-6矩阵往返误差、10.79mm首步平移越界而停止，保留原输出。只读全32条教师命令证实首步actual→command各轴最大13.87/17.48/20.26mm，相邻command最大5.68/7.11/8.14mm；旋转各轴仍在原0.1rad内。新五技能动作尺度改为25mm/轴，原关节0.1rad与tracking0.05rad不变；矩阵往返容差4e-6匹配float32 FK，不改IK位置/角度阈值。教师采集原始命令未修改。旧Reach回放由旧尺度解码物理值后再编码新尺度，旧checkpoint仍按旧尺度用于before基线；因此前后对照是扩展任务方案整体对照，不把结果单独归因于训练次数。
