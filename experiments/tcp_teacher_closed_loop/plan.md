# TCP 学生续训后的闭环对照

状态：2026-09-08 已完成，32/32 场景条件有记录，launcher 退出码 0。结果见 [results.md](results.md) 与 [summary.json](summary.json)；原始失败尝试保留，未追加训练或延长时长实验。

2026-09-08：用户批准执行此前讨论的闭环实验。比较原256次更新的TCP学生与续训最佳学生；不训练、不改架构或执行阈值，不自动获得物理控制权限。

固定既有 development 场景 `1600200–1600215`，共16个场景、每场景旧/新两个条件。先运行前四个场景的八个episode；链路有效后继续剩余十二个场景，不重跑首批。普通策略停止/失败保留并继续，输入身份、初态配对或共同运行链路错误停止后续计算，不更换seed直到成功。这是已使用development上的事后对照，不是独立final test。

旧/新共用原Qwen、Adapter、TCP/Memory输入与动作表达、BF16、固定场景/重规划seed、10步Euler、20Hz、16步chunk执行前4步、0.1 rad计划/修正限制、原0.05 rad实际跟踪检查、夹爪固定打开。主变量为checkpoint；续训曾重新初始化AdamW，不能严格归因为只有更新次数差异。

主指标是原2 cm Reach判定（每个真实控制步测量）与完成88步的比例。另报告执行步数、拒绝/停止原因、tracking error、Memory使用和配对共同前缀距离。不同停止时间的终点距离不作为公平效果排名。每个场景保留全部原始plans与control trace；初态/首帧/Memory必须严格配对，状态分叉后不声称输入继续相同。仿真GT目标仅供评估距离，不进入模型、Memory或动作生成。

加载新格式时验证冻结best checkpoint SHA、配置、父训练身份、更新数与全部state_dict；不修改旧训练格式或旧restore函数。先用两个固定教师窗口对照保存的预测，再执行仿真。渲染和六轴控制smoke为共同依赖，不以smoke通过代替任务成功。

单卡顺序执行，最多32个评估episode，每个最多88个policy控制步，另有六轴24步smoke和原warmup。运行外层设置1小时故障超时，超时保留部分结果，不冒充负结论或自动重复场景。源码使用原训练182文件快照加隔离新增runner与原0.1执行模块，旧源码/结果/权重保留。

结果按执行有效性与任务效果分别报告；只支持当前静态无碰撞Reach夹具的simulated closed-loop结论，不支持抓取、接触或物理部署。完成后再决定补数据、收尾训练或进一步执行诊断。

执行兼容性修复：第一条旧学生轨迹执行4步后，下一段计划因关节超限被拒绝；汇总器随后读取旧TraceController没有的派生字段 `tracking_error_rad` 而中断。原始command_q、q_after、plans与逐步距离完整保留。修复后从原始量重算指标，新运行身份恢复这一条既有停止结果，只执行其余31条，不重采样首条；原失败输出与SHA继续保留。这是统计日志兼容性修复，不改变控制或模型。

恢复前置检查又发现内存协议的tuple与JSON读回的list直接比较不等；已确认两份序列化协议及上游源码SHA完全相同，改用项目现有canonical JSON identity比较并补充序列化回归测试。该次失败尚未执行仿真动作；保留其独立失败记录，再继续同一已冻结场景清单。

同样的tuple/list差异也存在于恢复后的Memory初态记录。逐步原始记录核对显示旧/新warmup的输入digest、关节、TCP、距离和Memory内容一致（episode身份正常不同）；统一JSON表示后继续严格逐值配对，不增加任何数值容差。该检查失败时新学生只做了warmup，尚无policy动作。新增保存初态记录及恢复配对回归测试，保留失败尝试。
