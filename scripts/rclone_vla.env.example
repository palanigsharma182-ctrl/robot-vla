# 复制为仓库外的私有文件，再按当前机器修改并 source。
# OAuth token 不应写入此文件；它由 RCLONE_CONFIG 指向的 600 权限配置保存。

export RCLONE_CONFIG="/home/your-user/.config/rclone/rclone.conf"
export RCLONE_REMOTE="gdrive"

# 当前 WSL 需要代理时启用；端口应对应本机已建立的 SSH SOCKS 隧道。
# export RCLONE_HTTP_PROXY="socks5h://127.0.0.1:10809"
